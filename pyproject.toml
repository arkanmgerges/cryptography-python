[tool.poetry]
name = "cryptography"
version = "0.1.0"
description = ""
authors = ["Arkan M. Gerges <arkan.m.gerges@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pytest = "^7.4.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
